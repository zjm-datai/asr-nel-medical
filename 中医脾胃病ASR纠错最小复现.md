# 中医脾胃病 ASR 实体纠错最小复现

## 1. 复现目标

在中医脾胃病门诊这个窄场景中，验证生成式标注模块 `GenerativeLabeler`（GL）能否：

1. 根据音频、ASR 原文和候选实体，定位 ASR 原文中的错误实体片段。
2. 候选实体不正确时输出 `<empty>`，避免过纠正。
3. 相比纯拼音编辑距离纠错，提高实体纠正效果并降低误替换。

最小复现不训练 `SpeechSearcher`（SS），采用论文中的低成本变体 `PED+GL`：

```text
音频 + ASR 原文
  -> 汉字/拼音编辑距离召回 Top 5
  -> GL 逐候选输出错误片段或 <empty>
  -> 规则受控替换
  -> 纠错文本和审计日志
```

第一版只纠正实体名称，不处理剂量、单位、频次、煎服法，不生成诊断或处方，也不自由改写整句。

## 2. 实验对照

论文的主要强基线是 `PED-NEC`，即基于音素编辑距离的命名实体纠错。最小复现保留三个对照组：

| 实验组 | 候选召回 | 纠错方式 | 用途 |
| --- | --- | --- | --- |
| 原始 ASR | 无 | 不纠错 | 衡量 ASR 原始水平 |
| PED-NEC | 汉字/拼音编辑距离 | 编辑距离定位并规则替换 | 主要 baseline |
| PED+GL | 汉字/拼音编辑距离 | GL 标注错误片段，再规则替换 | 本次核心方法 |

暂不训练 SS。只有当正确实体的拼音召回 `Recall@5 < 95%` 时，再评估训练 SS 的必要性。

## 3. 场景和实体范围

场景限定为慢性胃炎、功能性消化不良等中医脾胃病门诊表达。

第一版实体库控制在 500 至 800 个实体：

| 类型 | 建议数量 | 示例 |
| --- | ---: | --- |
| 中药 | 200-300 | 白术、苍术、半夏、黄芩、黄连 |
| 方剂 | 100-150 | 半夏泻心汤、香砂六君子汤、保和丸 |
| 症状 | 100-150 | 脘腹胀满、嗳气、反酸、纳差 |
| 证型 | 50-80 | 脾胃虚弱、寒热错杂、肝胃不和 |
| 舌脉 | 50-120 | 舌淡苔白、脉弦、脉濡缓 |

其中先选约 150 个高频核心实体重点覆盖。

实体记录格式：

```json
{
  "entity_id": "formula_001",
  "canonical_name": "半夏泻心汤",
  "entity_type": "formula",
  "aliases": ["半夏泻心方"],
  "pinyin": "ban xia xie xin tang",
  "confusables": ["半夏厚朴汤"],
  "risk_level": "high"
}
```

必须人工维护易混实体组，例如：

```text
白术 / 苍术
黄芩 / 黄连 / 黄柏
枳实 / 枳壳
生地黄 / 熟地黄
赤芍 / 白芍
半夏泻心汤 / 半夏厚朴汤
理中丸 / 附子理中丸
香砂六君子汤 / 六君子汤
脾胃虚寒 / 胃阴不足
```

## 4. 最小数据规模

### 4.1 快速可行性实验

```text
原始音频：800-1,000 条
说话人：至少 20 人
核心实体：约 100 个
GL 配对样本：约 5,000 条
独立测试音频：至少 200 条
```

这个规模只能判断方案是否可行，不能证明达到上线水平。

### 4.2 推荐 MVP 规模

```text
原始音频：1,500-2,000 条
真实录音比例：至少 60%
说话人：至少 30 人
核心实体：约 150 个
总实体库：500-800 个
实体实例：至少 3,000 个
真实 ASR 错误实例：至少 1,000 个
GL 配对样本：8,000-12,000 条
独立测试音频：300-500 条
```

优先使用脱敏且取得授权的真实门诊音频，其次是多人朗读，最后用多音色 TTS 补充覆盖。训练集可以包含合成数据，但验证集和测试集应尽量使用真实录音。

## 5. 原始数据格式

人工参考数据：

```json
{
  "audio": "audio/00001.wav",
  "ref_text": "患者脘腹胀满嗳气处以半夏泻心汤加减",
  "entities": [
    {"text": "脘腹胀满", "type": "symptom"},
    {"text": "嗳气", "type": "symptom"},
    {"text": "半夏泻心汤", "type": "formula"}
  ]
}
```

使用最终准备部署的 ASR 引擎批量转写，保留未经人工修改的结果：

```json
{
  "audio": "audio/00001.wav",
  "ref_text": "患者脘腹胀满嗳气处以半夏泻心汤加减",
  "asr_text": "患者晚腹胀满爱气处以半夏谢心汤加减",
  "entities": [
    {"text": "脘腹胀满", "type": "symptom"},
    {"text": "嗳气", "type": "symptom"},
    {"text": "半夏泻心汤", "type": "formula"}
  ]
}
```

音频统一为单声道、16 kHz WAV。数据按说话人或原始音频切分为训练、验证、测试集，比例建议为 `70%/15%/15%`。同一音频衍生的所有候选配对必须位于同一个集合，防止数据泄漏。

## 6. GL 训练样本构造

GL 每次只判断一个候选实体，以降低训练和替换复杂度。

### 6.1 真实错误样本

```json
{
  "audio": "audio/00001.wav",
  "candidate": "半夏泻心汤",
  "asr_text": "患者服用半夏谢心汤后好转",
  "target": "半夏谢心汤"
}
```

### 6.2 实体已正确识别

```json
{
  "audio": "audio/00002.wav",
  "candidate": "半夏泻心汤",
  "asr_text": "患者服用半夏泻心汤后好转",
  "target": "半夏泻心汤"
}
```

推理时如果生成片段已经等于标准实体，则文本保持不变。

### 6.3 随机拒绝样本

```json
{
  "audio": "audio/00001.wav",
  "candidate": "保和丸",
  "asr_text": "患者服用半夏谢心汤后好转",
  "target": "<empty>"
}
```

### 6.4 易混实体拒绝样本

```json
{
  "audio": "audio/00001.wav",
  "candidate": "半夏厚朴汤",
  "asr_text": "患者服用半夏谢心汤后好转",
  "target": "<empty>"
}
```

推荐样本比例：

| 样本类型 | 比例 |
| --- | ---: |
| 真实 ASR 错误 | 35%-40% |
| TTS 或规则补充错误 | 15%-20% |
| 实体已正确识别 | 15% |
| 随机 `<empty>` | 10% |
| 易混实体 `<empty>` | 15%-20% |

`<empty>` 总比例保持在 25% 至 30%，其中至少一半是近音、同类或包含关系 hard negative。

参考文本和 ASR 文本通过字符级编辑对齐获得 `target`。自动生成后至少人工检查 500 条；错误片段边界准确率低于 95% 时，先修正对齐和标签，不开始正式训练。无法可靠对齐的样本直接丢弃。

## 7. GL 模型和训练配置

现有模型位于 `asr-nec-model/src/asr_nec_model/models/labeler.py`，采用 Whisper encoder-decoder。

输入 prompt：

```text
半夏泻心汤 <EC> 患者服用半夏谢心汤后好转
```

目标输出：

```text
半夏谢心汤
```

候选不应纠正时输出：

```text
<empty>
```

第一轮训练参数：

```text
基础模型：openai/whisper-base
Whisper encoder：冻结
decoder：微调
epoch：5
batch size：8-16
梯度累积：4-8
有效 batch size：至少 64
学习率：1e-4
优化器：AdamW
最大生成长度：32
混合精度：FP16 或 BF16
早停：验证指标连续 2 个 epoch 无提升
最佳模型指标：实体纠正 Precision 优先，其次 F1
```

正式训练前需要在现有工程中补齐：

```text
JSONL 数据加载
音频读取和 Whisper 特征缓存
训练/验证循环
混合精度和梯度累积
checkpoint 保存和断点续训
批量预测与评估命令
```

建议脚本：

```text
scripts/build_tcm_entity_lexicon.py
scripts/transcribe_audio.py
scripts/build_gl_dataset.py
scripts/cache_audio_features.py
scripts/train_gl.py
scripts/evaluate_gl.py
scripts/predict.py
```

## 8. PED 候选召回

第一版不训练 SS，从实体库按以下信号召回 Top 5：

```text
汉字编辑相似度：40%
拼音编辑相似度：40%
别名和关键词命中：20%
```

对召回结果逐个调用 GL，不把多个候选一次性塞入 prompt：

```text
候选“半夏泻心汤” -> GL 输出“半夏谢心汤” -> 可进入替换判断
候选“半夏厚朴汤” -> GL 输出 <empty> -> 拒绝
```

首先单独评估候选召回：正确实体进入 Top 5 才算命中。若 `Recall@5 >= 95%`，继续优化 GL；若明显低于 95%，优先补别名、拼音规范和召回算法，而不是盲目增加 GL 数据。

## 9. 受控替换和审计

只有同时满足以下条件才允许替换：

1. GL 输出不为空且不是 `<empty>`。
2. 输出是 ASR 原文中的连续子串。
3. 输出长度至少为两个汉字。
4. 候选实体存在于标准实体库。
5. 替换范围不覆盖否定词、剂量、单位、频次或煎服信息。
6. 多个候选不存在无法消解的片段冲突。

中药和方剂第一版默认进入人工审核，不直接写入正式病历。

审计日志格式：

```json
{
  "audio": "audio/00001.wav",
  "original_text": "服用半夏谢心汤后反酸减轻",
  "corrected_text": "服用半夏泻心汤后反酸减轻",
  "candidate": "半夏泻心汤",
  "generated_span": "半夏谢心汤",
  "entity_type": "formula",
  "retrieval_score": 0.91,
  "action": "review"
}
```

## 10. 评测指标和验收标准

不能只看总体 CER。至少报告：

```text
原始 ASR 的实体 Precision、Recall、F1
PED-NEC 的实体 Precision、Recall、F1
PED+GL 的实体 Precision、Recall、F1
候选召回 Recall@1、Recall@5
GL 错误片段完全匹配率
<empty> 拒绝 Precision、Recall
过纠正率
易混实体误替换率
中药、方剂、症状、证型、舌脉分类型指标
```

第一轮建议验收线：

```text
候选召回 Recall@5 >= 95%
PED+GL 实体纠正 Precision >= 90%
过纠正率 <= 2%
易混中药/方剂误替换率 <= 1%
相对原始 ASR 的实体错误减少 >= 30%
PED+GL 明显优于 PED-NEC baseline
```

医疗场景优先控制 Precision 和过纠正率，不以追求高 Recall 为由放宽危险替换。

## 11. 两周执行计划

### 第 1-2 天：定义范围和实体库

完成 500 至 800 个实体、约 150 个核心实体、别名、拼音和至少 100 组易混关系，由中医专业人员抽查标准名。

### 第 3-5 天：整理音频并运行 ASR

完成音频脱敏和授权检查，统一为 16 kHz WAV；制作人工参考文本和实体标注；使用目标 ASR 生成原始转写；按说话人切分训练、验证和测试集。

### 第 6-7 天：生成和检查 GL 数据

对齐参考文本与 ASR 文本，生成真实错误、正确实体、随机拒绝和易混拒绝样本。快速实验生成约 5,000 条，推荐 MVP 生成 8,000 至 12,000 条。人工检查至少 500 条。

### 第 8-9 天：补齐训练工程

实现数据加载、音频特征缓存、混合精度训练、梯度累积、验证评测、checkpoint 和批量推理入口。

### 第 10 天：第一轮训练

训练 Whisper-base GL 约 5 个 epoch，根据验证集 Precision 和 F1 选择最佳 checkpoint。重点检查 `<empty>` 拒绝能力和 hard negative 错误。

### 第 11-12 天：接入完整推理链路

完成汉字/拼音 Top-5 召回、GL 逐候选判断、规则替换、冲突处理和结构化审计日志。

### 第 13 天：测试和错误分析

在至少 200 条独立音频上比较原始 ASR、PED-NEC 和 PED+GL，按实体类型统计指标，并整理所有过纠正和易混实体错误。

### 第 14 天：专业评审和数据回流

由中医专业人员检查错误案例，按以下原因分类：

```text
实体库缺失或标准名错误
召回未找到正确实体
GL 未定位或定位边界错误
GL 未拒绝错误候选
ASR 错误过大，无法可靠纠正
替换规则误伤上下文
```

根据错误分类补充 1,000 至 2,000 条针对性样本，进行第二轮微调。

## 12. 最终交付物

最小复现完成后应包含：

```text
中医脾胃病标准实体库及易混关系
可复现的训练/验证/测试数据清单
PED-NEC baseline
训练后的 GL checkpoint
PED+GL 推理命令
受控替换器和审计日志
三组实验的评测报告
错误案例分类和下一轮数据清单
```

只有当 PED+GL 已达到验收标准、但候选召回仍是主要瓶颈时，才进入 SS 训练阶段。
