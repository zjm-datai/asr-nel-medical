# Generative Annotation NEC 论文复现与医学场景迁移方案

## 1. 论文方法一句话

这篇论文不是做一个通用文本纠错模型，而是做 ASR 后处理中的命名实体纠错：

```text
音频 + ASR 原文 + 领域实体库
  -> 检索音频中可能出现的实体
  -> 标注 ASR 文本中应该被替换的错误片段
  -> 用实体库标准名做受控替换
```

核心模块有三个：

1. `SpeechSearcher`：用整段语音和实体 TTS 语音做声学匹配，召回候选实体。
2. `GenerativeLabeler`：输入候选实体、ASR 文本和音频，生成 ASR 文本中的错误片段或 `<empty>`。
3. 规则替换器：只把 GL 标出的错误片段替换成候选实体，不让模型自由改写整句。

医学场景最适合使用这个方法的地方是：药品名、疾病名、检查项目、手术名、科室名、医生名、医院名等专业实体经常被 ASR 错转，且错词和正确实体的字面形式可能差很多。

例如：

```text
真实：二甲双胍缓释片
ASR：二甲双瓜缓释片 / 是甲双瓜缓释片

真实：糖化血红蛋白
ASR：糖化血容蛋白 / 糖化血红单白

真实：阿托伐他汀钙片
ASR：阿托法他丁钙片
```

## 2. 当前本地工程状态

当前目录下已有一个 `asr-nec-model` 原型工程，包含：

- `SpeechSearcher` 模型结构。
- `GenerativeLabeler` 模型结构。
- 候选实体检索和纠错 pipeline。
- 基础 dataset、collate、训练 loop 结构。

但它还不是完整可复现实验工程，缺少：

- 官方数据构造脚本。
- Aishell/Aishell-NER 标注对齐流程。
- Whisper ASR 生成错误转写的批处理流程。
- TTS 批量生成实体音频流程。
- SS 和 GL 的完整训练配置、评估脚本。
- 医学实体库、医学语音数据、医学场景评测集。

所以复现应分两层：

1. 先做论文最小复现：验证 SS+GL 流程能跑通。
2. 再做医学迁移：替换实体库和训练/评测数据，重点控制过纠正风险。

## 3. 论文复现路线

### 3.1 准备数据

论文复现至少需要四类数据：

```text
真实音频
人工参考文本 ref_text
ASR 原始转写 asr_text
实体库 entity_list
```

论文使用 Aishell 数据和实体标注构造训练样本。工程上可以按下面方式复现：

1. 下载 Aishell-1 音频和标注。
2. 获取或构造实体列表，例如人名、地名、组织名、专有名词。
3. 用 Whisper 生成每条音频的 ASR 原始转写。
4. 在参考文本中定位实体，判断该音频是否包含实体。
5. 用编辑距离或人工规则从 `asr_text` 中找出可能的错误片段，作为 GL 训练标签。

每条 GL 样本形如：

```json
{
  "audio": "xxx.wav",
  "candidates": ["二甲双胍片", "阿卡波糖"],
  "asr_text": "我昨天用了是二甲双瓜片",
  "target": "二甲双瓜片"
}
```

负样本形如：

```json
{
  "audio": "xxx.wav",
  "candidates": ["格列美脲"],
  "asr_text": "我昨天用了是二甲双瓜片",
  "target": "<empty>"
}
```

### 3.2 构建实体 datastore

对实体库中的每个实体：

```text
实体文本 -> TTS 合成音频 -> Whisper encoder -> CNN 投影 -> 声学表示
```

datastore 中应保存：

```json
{
  "entity": "二甲双胍片",
  "tts_audio": "entities/二甲双胍片.wav",
  "input_features": "Whisper 特征或 encoder 表示"
}
```

为了复现论文，TTS 可以先用一个稳定中文 TTS；为了医学迁移，建议每个实体至少生成多音色、多语速版本，否则实体检索容易对说话人和口音敏感。

### 3.3 训练 SpeechSearcher

SS 是二分类模型：

```text
输入：整段语音特征 + 某个实体 TTS 特征
输出：这段语音是否包含该实体
```

训练样本：

- 正样本：音频参考文本中包含该实体。
- 负样本：音频参考文本中不包含该实体。
- 论文负样本数量约为正样本 10 倍。

训练目标：

```text
BCEWithLogitsLoss
```

论文关键参数：

```text
Whisper-base encoder 冻结
CNN kernel_size = 3
CNN stride = 2
SAN dim = 512
FFN hidden = 2048
batch size = 512
learning rate = 5e-5
```

### 3.4 训练 GenerativeLabeler

GL 是基于 Whisper encoder-decoder 微调的生成式标注模型。

输入 prompt：

```text
候选实体1 ||| 候选实体2 <EC> ASR 原始文本
```

输出：

```text
错误片段1 ||| 错误片段2
```

如果候选实体不应纠正，则输出：

```text
<empty>
```

论文关键设计是：GL 不生成完整纠错文本，只生成要替换的错误片段。这样可以减少幻觉和过度改写。

训练时必须加入 `<empty>` 样本。论文约 20% prompt 放入不该纠正的实体或部分错误实体，用来训练拒绝能力。

### 3.5 推理流程

推理时：

```text
1. 输入用户音频，跑 ASR 得到 asr_text。
2. SS 在实体库中检索候选实体。
3. 保留概率 > 0.3 的实体，最多 top 5。
4. GL 根据候选实体、音频、asr_text 生成错误片段。
5. 规则替换错误片段为标准实体。
6. 输出纠错文本和替换日志。
```

替换日志建议保留：

```json
{
  "asr_text": "我昨天用了是二甲双瓜片",
  "candidate": "二甲双胍片",
  "score": 0.87,
  "generated_span": "二甲双瓜片",
  "corrected_text": "我昨天用了是二甲双胍片"
}
```

## 4. 医学场景迁移重点

医学场景不是简单把实体库换成药品词表。真正影响效果的是实体体系、数据构造、过纠正控制和评估指标。

### 4.1 医学实体库

建议先按实体类型拆库：

```text
药品名：通用名、商品名、剂型、规格
疾病名：标准诊断名、俗称、缩写
检查检验：项目名、英文缩写、中文读法
手术操作：术式名称
科室/医院/医生：机构和人员实体
医学缩写：CT、MRI、HbA1c、BNP、CRP、D-二聚体等
```

每个实体最好保留：

```json
{
  "canonical_name": "糖化血红蛋白",
  "aliases": ["HbA1c", "糖化血红素", "糖化血红蛋白A1c"],
  "entity_type": "lab_test",
  "risk_level": "high",
  "tts_forms": ["糖化血红蛋白", "H B A one C", "糖化血红蛋白 A 一 C"]
}
```

药品名尤其要处理：

- 通用名和商品名。
- 剂型：片、胶囊、注射液、缓释片、控释片。
- 规格和剂量：5mg、0.5g、每日两次。
- 近音药名：阿卡波糖/阿托伐他汀、格列齐特/格列美脲等。

### 4.2 医学训练数据

最理想的数据格式：

```text
医生/患者真实音频
人工校对文本
ASR 原始文本
医学实体标注
实体标准化结果
```

如果真实医学音频不足，可以分三阶段构造：

1. 合成冷启动数据：用医学文本模板 + TTS 生成。
2. 半真实数据：真实 ASR 文本 + 人工插入/替换医学实体 + TTS 或语音增强。
3. 真实闭环数据：上线后从人工审核日志中回收 ASR 错误和正确实体。

医学迁移必须专门构造多实体样本，因为医疗表达经常一句话里包含多个实体：

```text
患者昨天服用二甲双胍缓释片，今天复查糖化血红蛋白和空腹血糖。
```

训练集应覆盖：

- 一句话一个实体。
- 一句话多个实体。
- 同一实体重复出现。
- 实体只出现别名，输出标准名。
- 候选实体被召回但不应替换。
- 近音但语义不同的医学词。
- 药品名 + 剂量 + 频次混合表达。
- 英文缩写和中文读法混合表达。

### 4.3 医学评测集

不能只看整体 CER/WER。医学场景应至少评估：

```text
Overall CER/WER
Medical Entity CER
Medical Entity Recall
Medical Entity Precision
Over-correction Rate
High-risk Entity Recall
High-risk Entity False Correction Rate
```

特别要关注两个指标：

1. 高风险实体召回：真正说了“胰岛素”“华法林”“阿司匹林”等是否能纠出来。
2. 高风险误纠率：没有说这个药时，系统是否错误替换成该药。

医学场景中，过纠正比漏纠正更危险。建议上线默认策略是：

```text
高置信度：自动替换
中置信度：给出候选和替换日志，人工确认
低置信度：不替换
```

### 4.4 替换策略

论文的规则替换比较直接，但医学场景需要更保守。

建议替换前加约束：

- GL 输出片段必须是 ASR 原文中的连续子串。
- 替换片段长度不能过短，例如单字片段默认不替换。
- 同音普通词不能随意替换成高风险药品。
- 药品名替换不能吞掉剂量、频次、否定词。
- 出现“没有用”“停用”“过敏”等上下文时，保留完整上下文，不做语义改写。

例如不能把：

```text
没有吃二甲双瓜
```

改成：

```text
吃二甲双胍片
```

只能做实体片段替换：

```text
没有吃二甲双胍片
```

### 4.5 实体召回策略

论文 SS 是逐实体打分。医学实体库如果达到几万到几十万，逐个跑会慢。

可采用两级召回：

```text
第一级：拼音/音素/向量快速召回 top 100-500
第二级：SpeechSearcher 精排 top 5-20
第三极：GenerativeLabeler 拒绝和标注
```

这样既能控制延迟，也能利用论文方法处理“字面形式差很大”的错误。

## 5. 推荐落地路线

### 阶段 0：定义医学边界

先不要覆盖所有医学词。建议选一个窄场景：

```text
糖尿病随访
```

第一批实体：

- 常见降糖药。
- 胰岛素类型。
- 血糖相关检查。
- 常见并发症。
- 剂量、频次表达。

目标是先证明方法在一个高频场景里有效。

### 阶段 1：最小可用原型

输入：

```text
音频 wav
ASR 文本
1000-5000 个医学实体
```

输出：

```text
纠错后文本
候选实体
替换片段
置信度
替换日志
```

这一阶段可以先不完整训练 SS，而是用拼音/音素召回 + GL，先验证 GL 标注和拒绝能力。

### 阶段 2：训练医学 GL

构造 5k-20k 条医学 GL 样本：

- 真实 ASR 错误优先。
- 不足部分用 TTS 和错误模拟补充。
- 至少 20%-30% `<empty>` 负样本。
- 强化近音药品、检查缩写、多实体样本。

目标：

```text
降低过纠正
提升医学实体边界定位
```

### 阶段 3：训练/接入医学 SS

当实体库变大、拼音召回不够时，再训练 SS。

SS 数据构造：

```text
正样本：语音中包含该医学实体
负样本：同类型近音实体 + 随机实体 + 高频易混实体
```

医学负样本不能只随机采样，必须加入 hard negatives：

```text
阿卡波糖 vs 阿托伐他汀
格列齐特 vs 格列美脲
糖化血红蛋白 vs 血红蛋白
CT vs CTA
MRI vs MRA
```

### 阶段 4：安全上线

上线时不要只输出纠错文本，必须输出结构化审计信息：

```json
{
  "original_text": "...",
  "corrected_text": "...",
  "edits": [
    {
      "span": "二甲双瓜",
      "replacement": "二甲双胍片",
      "entity_type": "drug",
      "retrieval_score": 0.87,
      "labeler_score": 0.74,
      "action": "auto_replace"
    }
  ]
}
```

建议策略：

```text
药品、剂量、过敏史、诊断：默认人工可见
低风险科室/医院名：高置信度可自动替换
高风险低置信度：只提示，不自动替换
```

## 6. 关键风险

### 6.1 过纠正

医学场景最危险的问题是把普通词或另一个医学实体误替换成高风险实体。必须依赖：

- `<empty>` 负样本。
- hard negative。
- 替换日志。
- 置信度阈值。
- 人工审核策略。

### 6.2 实体标准化

论文只解决“纠成实体库里的标准字符串”，但医学实际还需要标准化到编码：

```text
药品 -> 药品编码/通用名/商品名
诊断 -> ICD 编码
检查 -> LOINC 或院内项目编码
手术 -> ICD-9-CM-3 或院内编码
```

所以 NEC 后面还需要接 NEL/entity linking，不应把 NEC 当成完整医学理解系统。

### 6.3 数据合规

真实医学音频涉及隐私和敏感健康信息。训练前需要：

- 脱敏。
- 授权。
- 访问控制。
- 数据留痕。
- 模型输出审计。

### 6.4 口音和说话风格

医学场景可能包含老人、方言、含混发音、嘈杂环境。实体 TTS 如果只有标准普通话，SS 泛化会受限。建议：

- 多音色 TTS。
- 语速扰动。
- 噪声增强。
- 真实口音样本补充。

## 7. 我建议的技术取舍

短期最务实方案：

```text
拼音/音素召回 + 医学 GL + 保守替换
```

原因是 SS 训练成本更高，且大实体库下逐实体声学匹配有延迟压力。先把 GL 的“错误片段标注”和“拒绝纠正”能力做出来，能最快验证医学价值。

中期方案：

```text
快速召回 top 100-500 + SS 精排 top 20 + GL 逐实体标注 + 审计替换
```

这更接近论文 SS+GL，同时适合大规模医学实体库。

长期方案：

```text
ASR N-best/时间戳 + 声学召回 + GL + 医学实体链接 + 人工反馈闭环
```

这样可以把纠错结果真正接入病历、随访、质控或问诊系统。

## 8. 最小实验清单

建议先做一个两周内可完成的验证实验：

```text
场景：糖尿病随访 ASR 纠错
实体：1000 个药品/检查/疾病实体
数据：500-1000 条真实或半合成音频
基线：Whisper 原始结果、拼音编辑距离纠错
模型：拼音召回 + GL
指标：医学实体 Recall、Precision、过纠正率
产物：纠错文本 + 替换日志 + 错误案例分析
```

验收标准建议：

```text
医学实体 Recall 明显高于基线
过纠正率低于可接受阈值
高风险实体不发生明显误替换
人工审核能看懂每次替换依据
```

如果这个实验成立，再投入训练 SS 和扩展实体库。
